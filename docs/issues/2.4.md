TITLE: 2.4 — Content migration script (Ghost export)
MILESTONE: M2 — Public marketing site
LABELS: devin,marketing
---
## Summary
One-time script to seed the blog from the Homestead Herds Ghost export — cattle/AI posts only.

## Spec references
- §8 (Content migration), §10b (Content migration) — binding.

## Requirements
- Source: `homestead-herds_ghost_2025-08-02-18-34-25.json` (87 posts).

## Acceptance criteria
- [ ] Migrate **only the listed cattle/AI posts** (§8 list); **leave** milk/dairy/donkey/recipe/herd-share content with Homestead Herds.
- [ ] Strip Ghost/Koenig markup → clean markdown/HTML.
- [ ] Migrated posts publish with **NEW dates** (look fresh).
- [ ] **Download and re-host images** in the new app — do **not** hotlink the old domain.
- [ ] Add **redirects from the old Homestead Herds URLs** for migrated posts that had traffic (store `posts.old_url`).
- [ ] One-time script, not load-bearing after.

## Tests
- [ ] Given a fixture export, only whitelisted posts import; images rewritten to local paths; redirects registered.

## Definition of done
Idempotent one-time importer; images re-hosted; redirects in place.
